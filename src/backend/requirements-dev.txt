-r requirements.txt

# Development & test dependencies for the Python 3 / Flask rewrite of the
# Hello World HTTP server. Installing this file
# (`pip install -r requirements-dev.txt`) pulls in the pinned runtime
# requirements above PLUS the test toolchain below, provisioning a complete
# dev/test environment in a single step. This supersedes the retired Node
# devDependencies (jest, supertest, eslint, prettier, nodemon).
#
# Versions are EXACT pins per Technical Specification (AAP) Section 0.4 and are
# verified installable from PyPI. Target interpreter: Python 3.12.

# Test runner - replaces Jest as the parity / unit / integration framework.
pytest==9.1.1

# Pytest fixtures for Flask apps. The ported integration suite also uses
# Flask's built-in test_client directly. Replaces SuperTest.
pytest-flask==1.3.0
