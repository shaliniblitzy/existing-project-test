# Runtime dependencies for the Python 3 / Flask rewrite of the Hello World HTTP server.
# Versions pinned per Technical Specification (AAP) Section 0.4 and verified installable on PyPI.
# Target interpreter: Python 3.12.

# Web framework (explicitly requested). Provides routing, request/response,
# error handlers, and the built-in test_client used by the parity test suite.
Flask==3.1.3

# WSGI toolkit / HTTP primitives. Transitive dependency of Flask 3.1 (requires Werkzeug>=3.1);
# pinned explicitly for reproducible builds.
Werkzeug==3.1.8

# Production WSGI HTTP server used to run the app in containers/production
# (invoked as: gunicorn wsgi:app --bind 0.0.0.0:3000).
gunicorn==26.0.0

# Loads environment variables from a .env file for local development
# (replaces the Node dotenv usage pattern; PORT / APP_ENV / LOG_LEVEL).
python-dotenv==1.2.2
